package com.rapleaf.hank.monitor.notification;

import org.apache.log4j.Logger;

public class SQLNotificationFormatter {
  private static Logger LOG = Logger.getLogger(SQLNotificationFormatter.class);
}
